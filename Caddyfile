{
    email {$EMAIL}
}

{$DOMAIN} {
    encode gzip zstd

    handle_path /geojson/* {
        reverse_proxy geojson:80
    }

    reverse_proxy metabase:3000

    log {
        output file /data/access.log {
            roll_size 10mb
            roll_keep 5
        }
    }
}
