name: france_aujourdhui
version: "0.1.0"
config-version: 2

profile: france_aujourdhui

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
asset-paths: ["assets"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  france_aujourdhui:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
